/** Replaced at bundle time by Vite `define` (see vite.config.ts). */
declare const __APP_BUILD_TIME__: string

/** ISO timestamp when this UI bundle was built. */
export const APP_BUILD_TIME: string = __APP_BUILD_TIME__
